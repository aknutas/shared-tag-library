package operations;

import data.*;
import java.util.Iterator;

public abstract class BookOperations {

	 /* 
	 * O(B) -> T[]:    //return a collection of tags similar to tags of input book
	 * O(S) -> T[]:    //return a collection of tags similar to tags of input bookshelf
	 * O(T) -> T[]:    //return a collection of tags similar to input tag
	 */
	
}
